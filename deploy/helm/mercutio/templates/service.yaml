apiVersion: v1
kind: Service
metadata:
  name: {{ include "mercutio.fullname" . }}
  labels:
    {{- include "mercutio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "mercutio.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
